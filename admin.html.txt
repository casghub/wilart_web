
```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wilart — Админ панель</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:ital,wght@0,400;1,400&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        brand: {
                            black: '#0a0a0a', dark: '#111111', darker: '#1a1a1a',
                            red: '#c41a20', 'red-accent': '#e31e25',
                            gold: '#b8956a', white: '#ffffff', cream: '#f8f6f3',
                            gray: '#888888', 'gray-light': '#cccccc',
                        }
                    },
                    fontFamily: { sans: ['Inter', 'sans-serif'], serif: ['Playfair Display', 'serif'] }
                }
            }
        }
    </script>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Inter', sans-serif; background: #0a0a0a; color: #fff; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #0a0a0a; }
        ::-webkit-scrollbar-thumb { background: #333; border-radius: 3px; }

        .sidebar-link { transition: all 0.2s ease; }
        .sidebar-link:hover, .sidebar-link.active {
            background: rgba(196, 26, 32, 0.1);
            border-left: 2px solid #c41a20;
            color: #fff;
        }
        .sidebar-link { border-left: 2px solid transparent; }

        .tab-content { display: none; }
        .tab-content.active { display: block; }

        .glass {
            background: rgba(255,255,255,0.03);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255,255,255,0.08);
        }
        .input-field {
            background: rgba(255,255,255,0.05);
            border: 1px solid rgba(255,255,255,0.1);
            color: #fff;
            transition: border-color 0.3s;
        }
        .input-field:focus { border-color: #c41a20; outline: none; }
        .input-field::placeholder { color: #555; }

        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
        .fade-in { animation: fadeIn 0.4s ease forwards; }

        .badge-new { background: rgba(196,26,32,0.2); color: #e31e25; }
        .badge-read { background: rgba(255,255,255,0.05); color: #888; }

        .stat-card { transition: transform 0.3s ease; }
        .stat-card:hover { transform: translateY(-4px); }

        /* Login Screen */
        #loginScreen {
            position: fixed; inset: 0; z-index: 100;
            background: #0a0a0a;
            display: flex; align-items: center; justify-content: center;
        }

        /* Notification */
        .notification {
            position: fixed; top: 24px; right: 24px; z-index: 200;
            transform: translateX(120%);
            transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .notification.show { transform: translateX(0); }

        .toggle-switch { position: relative; display: inline-block; width: 44px; height: 24px; }
        .toggle-switch input { opacity: 0; width: 0; height: 0; }
        .toggle-slider {
            position: absolute; cursor: pointer; inset: 0;
            background: #333; border-radius: 24px;
            transition: 0.3s;
        }
        .toggle-slider:before {
            content: ''; position: absolute;
            width: 18px; height: 18px; left: 3px; bottom: 3px;
            background: #888; border-radius: 50%; transition: 0.3s;
        }
        input:checked + .toggle-slider { background: rgba(196,26,32,0.3); }
        input:checked + .toggle-slider:before { transform: translateX(20px); background: #c41a20; }

        .drag-over { border-color: #c41a20 !important; background: rgba(196,26,32,0.05) !important; }
    </style>
</head>
<body>

<!-- ========== NOTIFICATION ========== -->
<div id="notification" class="notification">
    <div class="flex items-center gap-3 px-6 py-4 rounded-sm glass border border-white/10">
        <div id="notifIcon" class="w-8 h-8 flex items-center justify-center rounded-full"></div>
        <span id="notifText" class="text-sm font-medium"></span>
    </div>
</div>

<!-- ========== LOGIN SCREEN ========== -->
<div id="loginScreen">
    <div class="w-full max-w-md px-8 fade-in">
        <!-- Logo -->
        <div class="text-center mb-12">
            <div class="inline-flex items-center gap-3 mb-4">
                <div class="w-12 h-12 bg-brand-red flex items-center justify-center">
                    <span class="text-white font-serif text-2xl font-bold">W</span>
                </div>
                <div class="text-left">
                    <div class="font-serif text-2xl tracking-wider">WILART</div>
                    <div class="text-[10px] tracking-[0.3em] text-brand-gray uppercase">Панель управления</div>
                </div>
            </div>
        </div>

        <div class="glass rounded-sm p-10">
            <h2 class="text-xl font-medium mb-8 text-center">Вход в систему</h2>

            <form id="loginForm" class="space-y-6" onsubmit="handleLogin(event)">
                <div class="space-y-2">
                    <label class="text-xs tracking-widest uppercase text-brand-gray block">Логин</label>
                    <input type="text" id="loginUsername" placeholder="admin"
                        class="input-field w-full px-4 py-3 rounded-sm text-sm" required autocomplete="username">
                </div>
                <div class="space-y-2">
                    <label class="text-xs tracking-widest uppercase text-brand-gray block">Пароль</label>
                    <div class="relative">
                        <input type="password" id="loginPassword" placeholder="••••••••"
                            class="input-field w-full px-4 py-3 rounded-sm text-sm pr-12" required autocomplete="current-password">
                        <button type="button" onclick="togglePasswordVisibility()"
                            class="absolute right-4 top-1/2 -translate-y-1/2 text-brand-gray hover:text-white transition-colors">
                            <svg id="eyeIcon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                                <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/>
                            </svg>
                        </button>
                    </div>
                </div>
                <div id="loginError" class="hidden text-brand-red text-sm text-center py-2 bg-brand-red/10 rounded-sm">
                    Неверный логин или пароль
                </div>
                <button type="submit"
                    class="w-full bg-brand-red text-white py-4 tracking-widest uppercase text-sm font-medium hover:bg-brand-red-accent transition-colors">
                    Войти
                </button>
            </form>

            <div class="mt-8 pt-6 border-t border-white/5 text-center">
                <p class="text-brand-gray text-xs">По умолчанию: <span class="text-white">admin / wilart2025</span></p>
            </div>
        </div>
    </div>
</div>

<!-- ========== MAIN ADMIN LAYOUT ========== -->
<div id="adminLayout" class="hidden min-h-screen flex">

    <!-- SIDEBAR -->
    <aside id="sidebar" class="w-64 min-h-screen bg-brand-dark border-r border-white/5 flex flex-col fixed left-0 top-0 bottom-0 z-40 transition-transform duration-300">
        <!-- Logo -->
        <div class="px-6 py-6 border-b border-white/5">
            <div class="flex items-center gap-3">
                <div class="w-9 h-9 bg-brand-red flex items-center justify-center flex-shrink-0">
                    <span class="text-white font-serif text-lg font-bold">W</span>
                </div>
                <div>
                    <div class="font-serif text-base tracking-wider">WILART</div>
                    <div class="text-[8px] tracking-[0.3em] text-brand-gray uppercase">Управление</div>
                </div>
            </div>
        </div>

        <!-- Nav -->
        <nav class="flex-1 px-3 py-6 space-y-1 overflow-y-auto">
            <p class="text-[9px] tracking-[0.3em] uppercase text-brand-gray px-3 mb-3">Основное</p>

            <button onclick="switchTab('dashboard')"
                class="sidebar-link active w-full flex items-center gap-3 px-3 py-3 text-sm text-brand-gray-light rounded-sm" id="nav-dashboard">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                    <rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/>
                    <rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/>
                </svg>
                Дашборд
            </button>

            <button onclick="switchTab('leads')"
                class="sidebar-link w-full flex items-center gap-3 px-3 py-3 text-sm text-brand-gray rounded-sm" id="nav-leads">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                    <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                    <polyline points="22,6 12,13 2,6"/>
                </svg>
                Заявки
                <span id="newLeadsBadge" class="ml-auto badge-new text-xs px-2 py-0.5 rounded-full font-medium">3</span>
            </button>

            <button onclick="switchTab('content')"
                class="sidebar-link w-full flex items-center gap-3 px-3 py-3 text-sm text-brand-gray rounded-sm" id="nav-content">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                    <path d="M11 4H4a2 2 0 00-2 2v14a2 2 0 002 2h14a2 2 0 002-2v-7"/>
                    <path d="M18.5 2.5a2.121 2.121 0 013 3L12 15l-4 1 1-4 9.5-9.5z"/>
                </svg>
                Контент
            </button>

            <button onclick="switchTab('projects')"
                class="sidebar-link w-full flex items-center gap-3 px-3 py-3 text-sm text-brand-gray rounded-sm" id="nav-projects">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                    <rect x="3" y="3" width="18" height="18" rx="2"/>
                    <circle cx="8.5" cy="8.5" r="1.5"/>
                    <path d="M21 15l-5-5L5 21"/>
                </svg>
                Проекты
            </button>

            <button onclick="switchTab('reviews')"
                class="sidebar-link w-full flex items-center gap-3 px-3 py-3 text-sm text-brand-gray rounded-sm" id="nav-reviews">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                    <path d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z"/>
                </svg>
                Отзывы
            </button>

            <div class="pt-4 mt-4 border-t border-white/5">
                <p class="text-[9px] tracking-[0.3em] uppercase text-brand-gray px-3 mb-3">Система</p>

                <button onclick="switchTab('settings')"
                    class="sidebar-link w-full flex items-center gap-3 px-3 py-3 text-sm text-brand-gray rounded-sm" id="nav-settings">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                        <circle cx="12" cy="12" r="3"/>
                        <path d="M19.4 15a1.65 1.65 0 00.33 1.82l.06.06a2 2 0 010 2.83 2 2 0 01-2.83 0l-.06-.06a1.65 1.65 0 00-1.82-.33 1.65 1.65 0 00-1 1.51V21a2 2 0 01-4 0v-.09A1.65 1.65 0 009 19.4a1.65 1.65 0 00-1.82.33l-.06.06a2 2 0 01-2.83-2.83l.06-.06A1.65 1.65 0 004.68 15a1.65 1.65 0 00-1.51-1H3a2 2 0 010-4h.09A1.65 1.65 0 004.6 9a1.65 1.65 0 00-.33-1.82l-.06-.06a2 2 0 012.83-2.83l.06.06A1.65 1.65 0 009 4.68a1.65 1.65 0 001-1.51V3a2 2 0 014 0v.09a1.65 1.65 0 001 1.51 1.65 1.65 0 001.82-.33l.06-.06a2 2 0 012.83 2.83l-.06.06A1.65 1.65 0 0019.4 9a1.65 1.65 0 001.51 1H21a2 2 0 010 4h-.09a1.65 1.65 0 00-1.51 1z"/>
                    </svg>
                    Настройки
                </button>

                <a href="index.html" target="_blank"
                    class="sidebar-link w-full flex items-center gap-3 px-3 py-3 text-sm text-brand-gray rounded-sm">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                        <path d="M18 13v6a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2h6"/>
                        <polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/>
                    </svg>
                    Открыть сайт
                </a>
            </div>
        </nav>

        <!-- User -->
        <div class="px-4 py-4 border-t border-white/5">
            <div class="flex items-center gap-3">
                <div class="w-9 h-9 rounded-full bg-brand-red flex items-center justify-center text-xs font-bold">AD</div>
                <div class="flex-1 min-w-0">
                    <div class="text-sm font-medium truncate">Администратор</div>
                    <div class="text-xs text-brand-gray">admin@wilart.ru</div>
                </div>
                <button onclick="logout()" title="Выйти"
                    class="text-brand-gray hover:text-brand-red transition-colors p-1">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                        <path d="M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4"/>
                        <polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/>
                    </svg>
                </button>
            </div>
        </div>
    </aside>

    <!-- MAIN CONTENT -->
    <div class="ml-64 flex-1 flex flex-col min-h-screen">

        <!-- Top Bar -->
        <header class="sticky top-0 z-30 bg-brand-black/95 backdrop-blur-xl border-b border-white/5">
            <div class="flex items-center justify-between px-8 py-4">
                <div id="pageTitle" class="text-lg font-medium">Дашборд</div>
                <div class="flex items-center gap-4">
                    <div class="text-xs text-brand-gray" id="currentDateTime"></div>
                    <button onclick="switchTab('leads')"
                        class="relative p-2 text-brand-gray hover:text-white transition-colors">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                            <path d="M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9"/>
                            <path d="M13.73 21a2 2 0 01-3.46 0"/>
                        </svg>
                        <span class="absolute top-1 right-1 w-2 h-2 bg-brand-red rounded-full"></span>
                    </button>
                </div>
            </div>
        </header>

        <!-- TAB CONTENTS -->
        <main class="flex-1 p-8">

            <!-- ===== DASHBOARD ===== -->
            <div id="tab-dashboard" class="tab-content active fade-in">
                <div class="mb-8">
                    <h1 class="font-serif text-3xl text-white mb-2">Добро пожаловать 👋</h1>
                    <p class="text-brand-gray">Обзор активности сайта Wilart</p>
                </div>

                <!-- Stats Cards -->
                <div class="grid grid-cols-2 lg:grid-cols-4 gap-6 mb-10">
                    <div class="stat-card glass rounded-sm p-6">
                        <div class="flex items-center justify-between mb-4">
                            <div class="w-10 h-10 bg-brand-red/10 flex items-center justify-center">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#c41a20" stroke-width="1.5" stroke-linecap="round">
                                    <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                                    <polyline points="22,6 12,13 2,6"/>
                                </svg>
                            </div>
                            <span class="text-xs text-green-400 bg-green-400/10 px-2 py-1 rounded-full">+3 новых</span>
                        </div>
                        <div class="text-3xl font-serif text-white mb-1" id="totalLeads">12</div>
                        <div class="text-xs tracking-widest uppercase text-brand-gray">Всего заявок</div>
                    </div>

                    <div class="stat-card glass rounded-sm p-6">
                        <div class="flex items-center justify-between mb-4">
                            <div class="w-10 h-10 bg-brand-gold/10 flex items-center justify-center">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#b8956a" stroke-width="1.5" stroke-linecap="round">
                                    <rect x="3" y="3" width="18" height="18" rx="2"/>
                                    <circle cx="8.5" cy="8.5" r="1.5"/>
                                    <path d="M21 15l-5-5L5 21"/>
                                </svg>
                            </div>
                            <span class="text-xs text-brand-gold bg-brand-gold/10 px-2 py-1 rounded-full">активные</span>
                        </div>
                        <div class="text-3xl font-serif text-white mb-1" id="totalProjects">2</div>
                        <div class="text-xs tracking-widest uppercase text-brand-gray">Проекта</div>
                    </div>

                    <div class="stat-card glass rounded-sm p-6">
                        <div class="flex items-center justify-between mb-4">
                            <div class="w-10 h-10 bg-blue-500/10 flex items-center justify-center">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#60a5fa" stroke-width="1.5" stroke-linecap="round">
                                    <path d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z"/>
                                </svg>
                            </div>
                            <span class="text-xs text-blue-400 bg-blue-400/10 px-2 py-1 rounded-full">показаны</span>
                        </div>
                        <div class="text-3xl font-serif text-white mb-1" id="totalReviews">3</div>
                        <div class="text-xs tracking-widest uppercase text-brand-gray">Отзыва</div>
                    </div>

                    <div class="stat-card glass rounded-sm p-6">
                        <div class="flex items-center justify-between mb-4">
                            <div class="w-10 h-10 bg-green-500/10 flex items-center justify-center">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#4ade80" stroke-width="1.5" stroke-linecap="round">
                                    <polyline points="20 6 9 17 4 12"/>
                                </svg>
                            </div>
                            <span class="text-xs text-green-400 bg-green-400/10 px-2 py-1 rounded-full">онлайн</span>
                        </div>
                        <div class="text-3xl font-serif text-white mb-1">100%</div>
                        <div class="text-xs tracking-widest uppercase text-brand-gray">Сайт активен</div>
                    </div>
                </div>

                <!-- Recent Leads -->
                <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                    <div class="lg:col-span-2">
                        <div class="glass rounded-sm">
                            <div class="flex items-center justify-between px-6 py-5 border-b border-white/5">
                                <h3 class="font-medium">Последние заявки</h3>
                                <button onclick="switchTab('leads')" class="text-xs text-brand-red hover:text-white transition-colors tracking-widest uppercase">
                                    Все заявки →
                                </button>
                            </div>
                            <div id="recentLeadsList" class="divide-y divide-white/5"></div>
                        </div>
                    </div>

                    <!-- Quick Actions -->
                    <div class="space-y-4">
                        <div class="glass rounded-sm p-6">
                            <h3 class="font-medium mb-5 text-sm tracking-widest uppercase text-brand-gray">Быстрые действия</h3>
                            <div class="space-y-3">
                                <button onclick="switchTab('content')"
                                    class="w-full flex items-center gap-3 p-4 bg-brand-darker hover:bg-brand-red/10 transition-colors rounded-sm text-left group">
                                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#c41a20" stroke-width="1.5" stroke-linecap="round">
                                        <path d="M11 4H4a2 2 0 00-2 2v14a2 2 0 002 2h14a2 2 0 002-2v-7"/>
                                        <path d="M18.5 2.5a2.121 2.121 0 013 3L12 15l-4 1 1-4 9.5-9.5z"/>
                                    </svg>
                                    <div>
                                        <div class="text-sm font-medium group-hover:text-white transition-colors">Редактировать контент</div>
                                        <div class="text-xs text-brand-gray">Тексты и настройки</div>
                                    </div>
                                </button>
                                <button onclick="openAddProjectModal()"
                                    class="w-full flex items-center gap-3 p-4 bg-brand-darker hover:bg-brand-gold/10 transition-colors rounded-sm text-left group">
                                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#b8956a" stroke-width="1.5" stroke-linecap="round">
                                        <circle cx="12" cy="12" r="10"/>
                                        <line x1="12" y1="8" x2="12" y2="16"/>
                                        <line x1="8" y1="12" x2="16" y2="12"/>
                                    </svg>
                                    <div>
                                        <div class="text-sm font-medium group-hover:text-white transition-colors">Добавить проект</div>
                                        <div class="text-xs text-brand-gray">Новый кейс</div>
                                    </div>
                                </button>
                                <button onclick="exportLeads()"
                                    class="w-full flex items-center gap-3 p-4 bg-brand-darker hover:bg-green-500/10 transition-colors rounded-sm text-left group">
                                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#4ade80" stroke-width="1.5" stroke-linecap="round">
                                        <path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4"/>
                                        <polyline points="7 10 12 15 17 10"/>
                                        <line x1="12" y1="15" x2="12" y2="3"/>
                                    </svg>
                                    <div>
                                        <div class="text-sm font-medium group-hover:text-white transition-colors">Экспорт заявок</div>
                                        <div class="text-xs text-brand-gray">Скачать CSV</div>
                                    </div>
                                </button>
                            </div>
                        </div>

                        <!-- Site Status -->
                        <div class="glass rounded-sm p-6">
                            <h3 class="font-medium mb-5 text-sm tracking-widest uppercase text-brand-gray">Статус сайта</h3>
                            <div class="space-y-4">
                                <div class="flex items-center justify-between">
                                    <span class="text-sm text-brand-gray">Главная страница</span>
                                    <span class="flex items-center gap-2 text-xs text-green-400">
                                        <span class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></span>
                                        Активна
                                    </span>
                                </div>
                                <div class="flex items-center justify-between">
                                    <span class="text-sm text-brand-gray">Форма обратной связи</span>
                                    <span class="flex items-center gap-2 text-xs text-green-400">
                                        <span class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></span>
                                        Работает
                                    </span>
                                </div>
                                <div class="flex items-center justify-between">
                                    <span class="text-sm text-brand-gray">Последнее обновление</span>
                                    <span class="text-xs text-brand-gray" id="lastUpdate">Сегодня</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ===== LEADS ===== -->
            <div id="tab-leads" class="tab-content fade-in">
                <div class="flex items-center justify-between mb-8">
                    <div>
                        <h1 class="font-serif text-3xl text-white mb-2">Заявки</h1>
                        <p class="text-brand-gray">Входящие запросы с формы сайта</p>
                    </div>
                    <div class="flex items-center gap-4">
                        <button onclick="exportLeads()"
                            class="flex items-center gap-2 px-5 py-2.5 border border-white/20 hover:border-white/40 text-sm transition-colors">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                                <path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4"/>
                                <polyline points="7 10 12 15 17 10"/>
                                <line x1="12" y1="15" x2="12" y2="3"/>
                            </svg>
                            Экспорт CSV
                        </button>
                        <button onclick="clearAllLeads()"
                            class="flex items-center gap-2 px-5 py-2.5 border border-brand-red/30 hover:border-brand-red text-brand-red text-sm transition-colors">
                            Очистить всё
                        </button>
                    </div>
                </div>

                <!-- Filters -->
                <div class="flex items-center gap-4 mb-6">
                    <div class="flex items-center gap-2">
                        <button onclick="filterLeads('all')" id="filter-all"
                            class="filter-btn px-4 py-2 text-xs tracking-widest uppercase bg-brand-red text-white">
                            Все
                        </button>
                        <button onclick="filterLeads('new')" id="filter-new"
                            class="filter-btn px-4 py-2 text-xs tracking-widest uppercase bg-brand-darker text-brand-gray hover:text-white transition-colors">
                            Новые
                        </button>
                        <button onclick="filterLeads('read')" id="filter-read"
                            class="filter-btn px-4 py-2 text-xs tracking-widest uppercase bg-brand-darker text-brand-gray hover:text-white transition-colors">
                            Прочитанные
                        </button>
                    </div>
                    <div class="flex-1 max-w-xs relative">
                        <svg class="absolute left-3 top-1/2 -translate-y-1/2 text-brand-gray" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                            <circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/>
                        </svg>
                        <input type="text" id="leadsSearch" placeholder="Поиск по имени, email..." oninput="renderLeads()"
                            class="input-field w-full pl-10 pr-4 py-2 text-sm rounded-sm">
                    </div>
                </div>

                <div id="leadsContainer" class="space-y-3"></div>
            </div>

            <!-- ===== CONTENT ===== -->
            <div id="tab-content" class="tab-content fade-in">
                <div class="flex items-center justify-between mb-8">
                    <div>
                        <h1 class="font-serif text-3xl text-white mb-2">Редактор контента</h1>
                        <p class="text-brand-gray">Управление текстами и статистикой сайта</p>
                    </div>
                    <button onclick="saveContent()"
                        class="flex items-center gap-2 px-8 py-3 bg-brand-red text-white text-sm tracking-widest uppercase hover:bg-brand-red-accent transition-colors">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                            <path d="M19 21H5a2 2 0 01-2-2V5a2 2 0 012-2h11l5 5v11a2 2 0 01-2 2z"/>
                            <polyline points="17 21 17 13 7 13 7 21"/>
                            <polyline points="7 3 7 8 15 8"/>
                        </svg>
                        Сохранить
                    </button>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                    <!-- Hero Section -->
                    <div class="glass rounded-sm p-8">
                        <h3 class="font-medium mb-6 flex items-center gap-3">
                            <span class="w-6 h-6 bg-brand-red/20 flex items-center justify-center text-xs text-brand-red">H</span>
                            Главный экран (Hero)
                        </h3>
                        <div class="space-y-5">
                            <div class="space-y-2">
                                <label class="text-xs tracking-widest uppercase text-brand-gray">Заголовок</label>
                                <input id="heroTitle" type="text" value="Мебель для гостиниц & HoReCa"
                                    class="input-field w-full px-4 py-3 rounded-sm text-sm">
                            </div>
                            <div class="space-y-2">
                                <label class="text-xs tracking-widest uppercase text-brand-gray">Подзаголовок</label>
                                <textarea id="heroSubtitle" rows="3"
                                    class="input-field w-full px-4 py-3 rounded-sm text-sm resize-none">Премиальное оснащение коммерческих пространств. Собственное производство полного цикла.</textarea>
                            </div>
                            <div class="space-y-2">
                                <label class="text-xs tracking-widest uppercase text-brand-gray">Значок (слева от заголовка)</label>
                                <input id="heroBadge" type="text" value="С 2009 года"
                                    class="input-field w-full px-4 py-3 rounded-sm text-sm">
                            </div>
                            <div class="space-y-2">
                                <label class="text-xs tracking-widest uppercase text-brand-gray">Текст кнопки CTA</label>
                                <input id="heroCTA" type="text" value="Получить предложение"
                                    class="input-field w-full px-4 py-3 rounded-sm text-sm">
                            </div>
                        </div>
                    </div>

                    <!-- Stats -->
                    <div class="glass rounded-sm p-8">
                        <h3 class="font-medium mb-6 flex items-center gap-3">
                            <span class="w-6 h-6 bg-brand-gold/20 flex items-center justify-center text-xs text-brand-gold">S</span>
                            Статистика (цифры)
                        </h3>
                        <div class="space-y-5">
                            <div class="grid grid-cols-2 gap-4">
                                <div class="space-y-2">
                                    <label class="text-xs tracking-widest uppercase text-brand-gray">Лет опыта</label>
                                    <input id="statYears" type="number" value="15"
                                        class="input-field w-full px-4 py-3 rounded-sm text-sm">
                                </div>
                                <div class="space-y-2">
                                    <label class="text-xs tracking-widest uppercase text-brand-gray">Объектов</label>
                                    <input id="statObjects" type="number" value="150"
                                        class="input-field w-full px-4 py-3 rounded-sm text-sm">
                                </div>
                                <div class="space-y-2">
                                    <label class="text-xs tracking-widest uppercase text-brand-gray">м² производства</label>
                                    <input id="statArea" type="number" value="5000"
                                        class="input-field w-full px-4 py-3 rounded-sm text-sm">
                                </div>
                                <div class="space-y-2">
                                    <label class="text-xs tracking-widest uppercase text-brand-gray">Специалистов</label>
                                    <input id="statStaff" type="number" value="120"
                                        class="input-field w-full px-4 py-3 rounded-sm text-sm">
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- About -->
                    <div class="glass rounded-sm p-8">
                        <h3 class="font-medium mb-6 flex items-center gap-3">
                            <span class="w-6 h-6 bg-blue-500/20 flex items-center justify-center text-xs text-blue-400">A</span>
                            О компании
                        </h3>
                        <div class="space-y-5">
                            <div class="space-y-2">
                                <label class="text-xs tracking-widest uppercase text-brand-gray">Заголовок секции</label>
                                <input id="aboutTitle" type="text" value="Создаём пространство, в которое хочется вернуться"
                                    class="input-field w-full px-4 py-3 rounded-sm text-sm">
                            </div>
                            <div class="space-y-2">
                                <label class="text-xs tracking-widest uppercase text-brand-gray">Первый абзац</label>
                                <textarea id="aboutText1" rows="4"
                                    class="input-field w-full px-4 py-3 rounded-sm text-sm resize-none">Wilart — производственная компания с 15-летним опытом создания премиальной мебели для гостиничного и ресторанного бизнеса.</textarea>
                            </div>
                            <div class="space-y-2">
                                <label class="text-xs tracking-widest uppercase text-brand-gray">Второй абзац</label>
                                <textarea id="aboutText2" rows="4"
                                    class="input-field w-full px-4 py-3 rounded-sm text-sm resize-none">Собственное производство в Москве, команда из 120+ специалистов и портфолио из 150+ реализованных объектов.</textarea>
                            </div>
                        </div>
                    </div>

                    <!-- Contacts -->
                    <div class="glass rounded-sm p-8">
                        <h3 class="font-medium mb-6 flex items-center gap-3">
                            <span class="w-6 h-6 bg-green-500/20 flex items-center justify-center text-xs text-green-400">C</span>
                            Контактная информация
                        </h3>
                        <div class="space-y-5">
                            <div class="space-y-2">
                                <label class="text-xs tracking-widest uppercase text-brand-gray">Телефон</label>
                                <input id="contactPhone" type="text" value="+7 (800) 123-45-67"
                                    class="input-field w-full px-4 py-3 rounded-sm text-sm">
                            </div>
                            <div class="space-y-2">
                                <label class="text-xs tracking-widests uppercase text-brand-gray">Email</label>
                                <input id="contactEmail" type="email" value="info@wilart.ru"
                                    class="input-field w-full px-4 py-3 rounded-sm text-sm">
                            </div>
                            <div class="space-y-2">
                                <label class="text-xs tracking-widests uppercase text-brand-gray">Адрес</label>
                                <input id="contactAddress" type="text" value="Москва, ул. Производственная, 15"
                                    class="input-field w-full px-4 py-3 rounded-sm text-sm">
                            </div>
                            <div class="space-y-2">
                                <label class="text-xs tracking-widests uppercase text-brand-gray">Email для получения заявок</label>
                                <input id="notifyEmail" type="email" value="manager@wilart.ru"
                                    class="input-field w-full px-4 py-3 rounded-sm text-sm">
                                <p class="text-xs text-brand-gray">На этот адрес будут приходить уведомления о новых заявках</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ===== PROJECTS ===== -->
            <div id="tab-projects" class="tab-content fade-in">
                <div class="flex items-center justify-between mb-8">
                    <div>
                        <h1 class="font-serif text-3xl text-white mb-2">Проекты</h1>
                        <p class="text-brand-gray">Управление портфолио и кейсами</p>
                    </div>
                    <button onclick="openAddProjectModal()"
                        class="flex items-center gap-2 px-8 py-3 bg-brand-red text-white text-sm tracking-widest uppercase">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                            <circle cx="12" cy="12" r="10"/>
                            <line x1="12" y1="8" x2="12" y2="16"/>
                            <line x1="8" y1="12" x2="16" y2="12"/>
                        </svg>
                        Добавить проект
                    </button>
                </div>

                <div id="projectsGrid" class="grid grid-cols-1 md:grid-cols-2 gap-6"></div>
            </div>

            <!-- ===== REVIEWS ===== -->
            <div id="tab-reviews" class="tab-content fade-in">
                <div class="flex items-center justify-between mb-8">
                    <div>
                        <h1 class="font-serif text-3xl text-white mb-2">Отзывы</h1>
                        <p class="text-brand-gray">Управление отзывами клиентов</p>
                    </div>
                    <button onclick="openAddReviewModal()"
                        class="flex items-center gap-2 px-8 py-3 bg-brand-red text-white text-sm tracking-widest uppercase">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                            <circle cx="12" cy="12" r="10"/>
                            <line x1="12" y1="8" x2="12" y2="16"/>
                            <line x1="8" y1="12" x2="16" y2="12"/>
                        </svg>
                        Добавить отзыв
                    </button>
                </div>

                <div id="reviewsGrid" class="grid grid-cols-1 md:grid-cols-3 gap-6"></div>
            </div>

            <!-- ===== SETTINGS ===== -->
            <div id="tab-settings" class="tab-content fade-in">
                <div class="mb-8">
                    <h1 class="font-serif text-3xl text-white mb-2">Настройки</h1>
                    <p class="text-brand-gray">Конфигурация системы и безопасность</p>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                    <!-- Change Password -->
                    <div class="glass rounded-sm p-8">
                        <h3 class="font-medium mb-6 flex items-center gap-3">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#c41a20" stroke-width="1.5" stroke-linecap="round">
                                <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
                                <path d="M7 11V7a5 5 0 0110 0v4"/>
                            </svg>
                            Смена пароля
                        </h3>
                        <form onsubmit="changePassword(event)" class="space-y-5">
                            <div class="space-y-2">
                                <label class="text-xs tracking-widests uppercase text-brand-gray">Текущий пароль</label>
                                <input type="password" id="currentPwd" class="input-field w-full px-4 py-3 rounded-sm text-sm" placeholder="••••••••" required>
                            </div>
                            <div class="space-y-2">
                                <label class="text-xs tracking-widests uppercase text-brand-gray">Новый пароль</label>
                                <input type="password" id="newPwd" class="input-field w-full px-4 py-3 rounded-sm text-sm" placeholder="Минимум 8 символов" required minlength="8">
                            </div>
                            <div class="space-y-2">
                                <label class="text-xs tracking-widests uppercase text-brand-gray">Повторите пароль</label>
                                <input type="password" id="confirmPwd" class="input-field w-full px-4 py-3 rounded-sm text-sm" placeholder="••••••••" required>
                            </div>
                            <button type="submit" class="w-full bg-brand-red text-white py-3 text-sm tracking-widest uppercase hover:bg-brand-red-accent transition-colors">
                                Обновить пароль
                            </button>
                        </form>
                    </div>

                    <!-- Notifications -->
                    <div class="glass rounded-sm p-8">
                        <h3 class="font-medium mb-6 flex items-center gap-3">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#b8956a" stroke-width="1.5" stroke-linecap="round">
                                <path d="M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9"/>
                                <path d="M13.73 21a2 2 0 01-3.46 0"/>
                            </svg>
                            Уведомления
                        </h3>
                        <div class="space-y-5">
                            <div class="flex items-center justify-between py-4 border-b border-white/5">
                                <div>
                                    <div class="text-sm font-medium">Новые заявки</div>
                                    <div class="text-xs text-brand-gray mt-1">Уведомление при получении заявки</div>
                                </div>
                                <label class="toggle-switch">
                                    <input type="checkbox" checked>
                                    <span class="toggle-slider"></span>
                                </label>
                            </div>
                            <div class="flex items-center justify-between py-4 border-b border-white/5">
                                <div>
                                    <div class="text-sm font-medium">Email уведомления</div>
                                    <div class="text-xs text-brand-gray mt-1">Дублировать на почту</div>
                                </div>
                                <label class="toggle-switch">
                                    <input type="checkbox">
                                    <span class="toggle-slider"></span>
                                </label>
                            </div>
                            <div class="flex items-center justify-between py-4">
                                <div>
                                    <div class="text-sm font-medium">Звуковые уведомления</div>
                                    <div class="text-xs text-brand-gray mt-1">Звук при новой заявке</div>
                                </div>
                                <label class="toggle-switch">
                                    <input type="checkbox" checked>
                                    <span class="toggle-slider"></span>
                                </label>
                            </div>
                        </div>
                    </div>

                    <!-- Danger Zone -->
                    <div class="glass rounded-sm p-8 border border-brand-red/20">
                        <h3 class="font-medium mb-6 flex items-center gap-3 text-brand-red">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                                <path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/>
                                <line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>
                            </svg>
                            Опасная зона
                        </h3>
                        <div class="space-y-4">
                            <div class="flex items-center justify-between p-4 bg-brand-red/5 rounded-sm">
                                <div>
                                    <div class="text-sm font-medium">Сбросить все данные</div>
                                    <div class="text-xs text-brand-gray mt-1">Удалит все заявки и проекты</div>
                                </div>
                                <button onclick="resetAllData()"
                                    class="px-4 py-2 border border-brand-red text-brand-red text-xs tracking-widests uppercase hover:bg-brand-red hover:text-white transition-all">
                                    Сбросить
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- About System -->
                    <div class="glass rounded-sm p-8">
                        <h3 class="font-medium mb-6">О системе</h3>
                        <div class="space-y-3 text-sm">
                            <div class="flex justify-between py-2 border-b border-white/5">
                                <span class="text-brand-gray">Версия</span>
                                <span class="text-white">1.0.0</span>
                            </div>
                            <div class="flex justify-between py-2 border-b border-white/5">
                                <span class="text-brand-gray">Хранилище</span>
                                <span class="text-white">localStorage</span>
                            </div>
                            <div class="flex justify-between py-2 border-b border-white/5">
                                <span class="text-brand-gray">Проект</span>
                                <span class="text-white">Wilart CMS</span>
                            </div>
                            <div class="flex justify-between py-2">
                                <span class="text-brand-gray">Дата установки</span>
                                <span class="text-white" id="installDate">—</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </main>
    </div>
</div>

<!-- ===== MODALS ===== -->

<!-- Add/Edit Project Modal -->
<div id="projectModal" class="hidden fixed inset-0 z-50 bg-black/80 backdrop-blur-sm flex items-center justify-center p-4">
    <div class="glass w-full max-w-2xl rounded-sm p-8 max-h-[90vh] overflow-y-auto">
        <div class="flex items-center justify-between mb-8">
            <h2 class="font-serif text-2xl" id="projectModalTitle">Добавить проект</h2>
            <button onclick="closeModal('projectModal')" class="text-brand-gray hover:text-white transition-colors">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                    <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
                </svg>
            </button>
        </div>
        <form id="projectForm" onsubmit="saveProject(event)" class="space-y-6">
            <input type="hidden" id="projectId">
            <div class="grid grid-cols-2 gap-6">
                <div class="space-y-2">
                    <label class="text-xs tracking-widests uppercase text-brand-gray">Название</label>
                    <input id="projectName" type="text" placeholder="Апарт-отель «Москва Сити»" required
                        class="input-field w-full px-4 py-3 rounded-sm text-sm">
                </div>
                <div class="space-y-2">
                    <label class="text-xs tracking-widests uppercase text-brand-gray">Тип объекта</label>
                    <select id="projectType" class="input-field w-full px-4 py-3 rounded-sm text-sm">
                        <option value="Апарт-отель">Апарт-отель</option>
                        <option value="Бутик-отель">Бутик-отель</option>
                        <option value="Гостиница">Гостиница</option>
                        <option value="Ресторан">Ресторан</option>
                        <option value="Офис">Офис</option>
                    </select>
                </div>
            </div>
            <div class="grid grid-cols-3 gap-4">
                <div class="space-y-2">
                    <label class="text-xs tracking-widests uppercase text-brand-gray">Год</label>
                    <input id="projectYear" type="number" placeholder="2024" value="2024"
                        class="input-field w-full px-4 py-3 rounded-sm text-sm">
                </div>
                <div class="space-y-2">
                    <label class="text-xs tracking-widests uppercase text-brand-gray">Кол-во номеров</label>
                    <input id="projectRooms" type="number" placeholder="84"
                        class="input-field w-full px-4 py-3 rounded-sm text-sm">
                </div>
                <div class="space-y-2">
                    <label class="text-xs tracking-widests uppercase text-brand-gray">Срок (дней)</label>
                    <input id="projectDays" type="number" placeholder="45"
                        class="input-field w-full px-4 py-3 rounded-sm text-sm">
                </div>
            </div>
            <div class="space-y-2">
                <label class="text-xs tracking-widests uppercase text-brand-gray">Описание</label>
                <textarea id="projectDesc" rows="4" placeholder="Описание проекта..."
                    class="input-field w-full px-4 py-3 rounded-sm text-sm resize-none"></textarea>
            </div>
            <div class="space-y-2">
                <label class="text-xs tracking-widests uppercase text-brand-gray">Теги (через запятую)</label>
                <input id="projectTags" type="text" placeholder="Кровати, Шкафы, Ресепшен"
                    class="input-field w-full px-4 py-3 rounded-sm text-sm">
            </div>
            <div class="flex items-center gap-4 pt-4">
                <button type="submit"
                    class="flex-1 bg-brand-red text-white py-4 text-sm tracking-widest uppercase hover:bg-brand-red-accent transition-colors">
                    Сохранить
                </button>
                <button type="button" onclick="closeModal('projectModal')"
                    class="px-8 py-4 border border-white/20 text-sm text-brand-gray hover:text-white hover:border-white/40 transition-colors">
                    Отмена
                </button>
            </div>
        </form>
    </div>
</div>

<!-- Add/Edit Review Modal -->
<div id="reviewModal" class="hidden fixed inset-0 z-50 bg-black/80 backdrop-blur-sm flex items-center justify-center p-4">
    <div class="glass w-full max-w-lg rounded-sm p-8">
        <div class="flex items-center justify-between mb-8">
            <h2 class="font-serif text-2xl" id="reviewModalTitle">Добавить отзыв</h2>
            <button onclick="closeModal('reviewModal')" class="text-brand-gray hover:text-white transition-colors">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                    <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
                </svg>
            </button>
        </div>
        <form id="reviewForm" onsubmit="saveReview(event)" class="space-y-5">
            <input type="hidden" id="reviewId">
            <div class="space-y-2">
                <label class="text-xs tracking-widests uppercase text-brand-gray">Имя клиента</label>
                <input id="reviewName" type="text" placeholder="Иван Петров" required
                    class="input-field w-full px-4 py-3 rounded-sm text-sm">
            </div>
            <div class="space-y-2">
                <label class="text-xs tracking-widests uppercase text-brand-gray">Должность и компания</label>
                <input id="reviewPosition" type="text" placeholder='Управляющий, Отель "Азимут"'
                    class="input-field w-full px-4 py-3 rounded-sm text-sm">
            </div>
            <div class="space-y-2">
                <label class="text-xs tracking-widests uppercase text-brand-gray">Оценка</label>
                <select id="reviewStars" class="input-field w-full px-4 py-3 rounded-sm text-sm">
                    <option value="5">★★★★★ — 5 звёзд</option>
                    <option value="4">★★★★☆ — 4 звезды</option>
                    <option value="3">★★★☆☆ — 3 звезды</option>
                </select>
            </div>
            <div class="space-y-2">
                <label class="text-xs tracking-widests uppercase text-brand-gray">Текст отзыва</label>
                <textarea id="reviewText" rows="4" placeholder="Текст отзыва..." required
                    class="input-field w-full px-4 py-3 rounded-sm text-sm resize-none"></textarea>
            </div>
            <div class="flex items-center gap-4 pt-4">
                <button type="submit"
                    class="flex-1 bg-brand-red text-white py-4 text-sm tracking-widest uppercase">
                    Сохранить
                </button>
                <button type="button" onclick="closeModal('reviewModal')"
                    class="px-8 py-4 border border-white/20 text-sm text-brand-gray hover:text-white transition-colors">
                    Отмена
                </button>
            </div>
        </form>
    </div>
</div>

<!-- Lead Detail Modal -->
<div id="leadModal" class="hidden fixed inset-0 z-50 bg-black/80 backdrop-blur-sm flex items-center justify-center p-4">
    <div class="glass w-full max-w-lg rounded-sm p-8">
        <div class="flex items-center justify-between mb-8">
            <h2 class="font-serif text-2xl">Детали заявки</h2>
            <button onclick="closeModal('leadModal')" class="text-brand-gray hover:text-white transition-colors">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                    <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
                </svg>
            </button>
        </div>
        <div id="leadDetail" class="space-y-5"></div>
    </div>
</div>

<script>
// =============================================
//  DATA STORE (localStorage)
// =============================================
const DB = {
    get: (key, def) => { try { return JSON.parse(localStorage.getItem('wilart_' + key)) ?? def; } catch { return def; } },
    set: (key, val) => localStorage.setItem('wilart_' + key, JSON.stringify(val)),
};

// Default leads (demo)
const DEFAULT_LEADS = [
    { id: 1, name: 'Алексей Волков', phone: '+7 (916) 234-56-78', email: 'a.volkov@hotel.ru', project: 'Оснащение 50 номеров гостиницы в Сочи, 3 звезды', date: '2025-06-10 14:30', status: 'new' },
    { id: 2, name: 'Марина Соколова', phone: '+7 (903) 456-78-90', email: 'marina@restoran.ru', project: 'Ресторан на 150 посадочных мест, авторский дизайн', date: '2025-06-09 11:15', status: 'new' },
    { id: 3, name: 'Сергей Казаков', phone: '+7 (926) 789-01-23', email: '', project: 'Апарт-отель 120 номеров, типовое оснащение', date: '2025-06-08 09:45', status: 'new' },
    { id: 4, name: 'Оксана Нестерова', phone: '+7 (985) 111-22-33', email: 'oknes@spa.ru', project: 'SPA-комплекс, лаунж зона, ресепшен', date: '2025-06-05 16:20', status: 'read' },
    { id: 5, name: 'Игорь Попов', phone: '+7 (977) 444-55-66', email: 'igor@boutique.ru', project: 'Бутик-отель 24 номера, премиум сегмент', date: '2025-06-03 13:00', status: 'read' },
];

const DEFAULT_PROJECTS = [
    { id: 1, name: 'Апарт-отель «Москва Сити»', type: 'Апарт-отель', year: 2024, rooms: 84, days: 45, desc: 'Полный цикл оснащения 84 номеров апарт-отеля премиум-класса.', tags: 'Кровати,Шкафы,Кухонные блоки,Ресепшен' },
    { id: 2, name: 'Бутик-отель «Невский»', type: 'Бутик-отель', year: 2023, rooms: 32, days: 60, desc: 'Комплексное оснащение бутик-отеля в историческом центре Санкт-Петербурга.', tags: 'Мебель номеров,Ресторан,Лобби,SPA' },
];

const DEFAULT_REVIEWS = [
    { id: 1, name: 'Иван Петров', position: 'Управляющий, Отель «Азимут»', stars: 5, text: 'Компания Wilart выполнила оснащение 120 номеров в установленные сроки. Качество мебели превзошло наши ожидания.' },
    { id: 2, name: 'Анна Козлова', position: 'Архитектор, Апарт-отель «СитиВью»', stars: 5, text: 'Работали с Wilart по дизайн-проекту. Точное соответствие чертежам, соблюдение сроков.' },
    { id: 3, name: 'Дмитрий Смирнов', position: 'Владелец, Ресторан «Волна»', stars: 5, text: 'Заказывали мебель для ресторана на 200 посадочных мест. Индивидуальный подход, нестандартные решения.' },
];

// Init default data
if (!DB.get('leads', null)) DB.set('leads', DEFAULT_LEADS);
if (!DB.get('projects', null)) DB.set('projects', DEFAULT_PROJECTS);
if (!DB.get('reviews', null)) DB.set('reviews', DEFAULT_REVIEWS);
if (!DB.get('credentials', null)) DB.set('credentials', { username: 'admin', password: 'wilart2025' });
if (!DB.get('installDate', null)) DB.set('installDate', new Date().toLocaleDateString('ru-RU'));

// =============================================
//  AUTH
// =============================================
function handleLogin(e) {
    e.preventDefault();
    const creds = DB.get('credentials', {});
    const user = document.getElementById('loginUsername').value;
    const pass = document.getElementById('loginPassword').value;
    if (user === creds.username && pass === creds.password) {
        document.getElementById('loginScreen').style.display = 'none';
        document.getElementById('adminLayout').classList.remove('hidden');
        initAdmin();
    } else {
        document.getElementById('loginError').classList.remove('hidden');
        setTimeout(() => document.getElementById('loginError').classList.add('hidden'), 3000);
    }
}

function logout() {
    if (confirm('Выйти из панели управления?')) {
        document.getElementById('adminLayout').classList.add('hidden');
        document.getElementById('loginScreen').style.display = 'flex';
        document.getElementById('loginUsername').value = '';
        document.getElementById('loginPassword').value = '';
    }
}

function togglePasswordVisibility() {
    const input = document.getElementById('loginPassword');
    input.type = input.type === 'password' ? 'text' : 'password';
}

function changePassword(e) {
    e.preventDefault();
    const creds = DB.get('credentials', {});
    const cur = document.getElementById('currentPwd').value;
    const newP = document.getElementById('newPwd').value;
    const conf = document.getElementById('confirmPwd').value;
    if (cur !== creds.password) return notify('Неверный текущий пароль', 'error');
    if (newP !== conf) return notify('Пароли не совпадают', 'error');
    DB.set('credentials', { ...creds, password: newP });
    notify('Пароль успешно изменён', 'success');
    e.target.reset();
}

// =============================================
//  INIT
// =============================================
function initAdmin() {
    updateDateTime();
    setInterval(updateDateTime, 1000);
    document.getElementById('installDate').textContent = DB.get('installDate', '—');
    renderDashboard();
    renderLeads();
    renderProjects();
    renderReviews();
    loadContent();
}

function updateDateTime() {
    const now = new Date();
    document.getElementById('currentDateTime').textContent =
        now.toLocaleDateString('ru-RU') + ' ' + now.toLocaleTimeString('ru-RU', {hour:'2-digit', minute:'2-digit'});
}

// =============================================
//  TABS
// =============================================
const TAB_NAMES = {
    dashboard: 'Дашборд', leads: 'Заявки', content: 'Редактор контента',
    projects: 'Проекты', reviews: 'Отзывы', settings: 'Настройки'
};

function switchTab(tab) {
    document.querySelectorAll('.tab-content').forEach(t => { t.classList.remove('active'); });
    document.querySelectorAll('.sidebar-link').forEach(l => l.classList.remove('active', 'text-white'));
    const content = document.getElementById('tab-' + tab);
    if (content) { content.classList.add('active'); content.classList.add('fade-in'); }
    const nav = document.getElementById('nav-' + tab);
    if (nav) nav.classList.add('active');
    document.getElementById('pageTitle').textContent = TAB_NAMES[tab] || tab;
}

// =============================================
//  NOTIFICATION
// =============================================
function notify(msg, type = 'success') {
    const el = document.getElementById('notification');
    const icon = document.getElementById('notifIcon');
    const text = document.getElementById('notifText');
    text.textContent = msg;
    if (type === 'success') {
        icon.style.background = 'rgba(74,222,128,0.1)';
        icon.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#4ade80" stroke-width="2" stroke-linecap="round"><polyline points="20 6 9 17 4 12"/></svg>';
    } else {
        icon.style.background = 'rgba(196,26,32,0.1)';
        icon.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#c41a20" stroke-width="2" stroke-linecap="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>';
    }
    el.classList.add('show');
    setTimeout(() => el.classList.remove('show'), 3500);
}

// =============================================
//  DASHBOARD
// =============================================
function renderDashboard() {
    const leads = DB.get('leads', []);
    const newCount = leads.filter(l => l.status === 'new').length;
    document.getElementById('totalLeads').textContent = leads.length;
    document.getElementById('totalProjects').textContent = DB.get('projects', []).length;
    document.getElementById('totalReviews').textContent = DB.get('reviews', []).length;
    document.getElementById('newLeadsBadge').textContent = newCount;
    const recent = leads.slice(-5).reverse();
    const container = document.getElementById('recentLeadsList');
    if (!recent.length) { container.innerHTML = '<div class="p-6 text-brand-gray text-sm text-center">Заявок пока нет</div>'; return; }
    container.innerHTML = recent.map(l => `
        <div class="flex items-center justify-between px-6 py-4 hover:bg-white/2 transition-colors cursor-pointer" onclick="openLeadDetail(${l.id})">
            <div class="flex items-center gap-4">
                <div class="w-9 h-9 rounded-full bg-brand-darker flex items-center justify-center text-xs font-medium text-brand-gray">
                    ${l.name.split(' ').map(n=>n[0]).join('').slice(0,2)}
                </div>
                <div>
                    <div class="text-sm font-medium">${l.name}</div>
                    <div class="text-xs text-brand-gray truncate max-w-[200px]">${l.project.substring(0,50)}...</div>
                </div>
            </div>
            <div class="flex items-center gap-3">
                <span class="text-xs text-brand-gray">${l.date.split(' ')[0]}</span>
                <span class="text-xs px-2 py-1 rounded-full ${l.status==='new' ? 'badge-new' : 'badge-read'}">${l.status==='new' ? 'Новая' : 'Прочитано'}</span>
            </div>
        </div>`).join('');
    document.getElementById('lastUpdate').textContent = new Date().toLocaleDateString('ru-RU');
}

// =============================================
//  LEADS
// =============================================
let currentFilter = 'all';

function filterLeads(filter) {
    currentFilter = filter;
    document.querySelectorAll('.filter-btn').forEach(b => {
        b.classList.remove('bg-brand-red', 'text-white');
        b.classList.add('bg-brand-darker', 'text-brand-gray');
    });
    document.getElementById('filter-' + filter).classList.add('bg-brand-red', 'text-white');
    document.getElementById('filter-' + filter).classList.remove('bg-brand-darker', 'text-brand-gray');
    renderLeads();
}

function renderLeads() {
    let leads = DB.get('leads', []);
    const search = document.getElementById('leadsSearch')?.value?.toLowerCase() || '';
    if (currentFilter === 'new') leads = leads.filter(l => l.status === 'new');
    if (currentFilter === 'read') leads = leads.filter(l => l.status === 'read');
    if (search) leads = leads.filter(l => l.name.toLowerCase().includes(search) || l.email?.toLowerCase().includes(search) || l.project.toLowerCase().includes(search));
    const container = document.getElementById('leadsContainer');
    if (!leads.length) { container.innerHTML = '<div class="glass rounded-sm p-12 text-center text-brand-gray">Заявок не найдено</div>'; return; }
    container.innerHTML = leads.reverse().map(l => `
        <div class="glass rounded-sm p-6 hover:border-brand-red/20 transition-colors cursor-pointer group" onclick="openLeadDetail(${l.id})">
            <div class="flex items-start justify-between gap-4">
                <div class="flex items-start gap-5 flex-1 min-w-0">
                    <div class="w-11 h-11 rounded-full bg-brand-darker flex items-center justify-center text-sm font-medium text-brand-gray flex-shrink-0">
                        ${l.name.split(' ').map(n=>n[0]).join('').slice(0,2)}
                    </div>
                    <div class="flex-1 min-w-0">
                        <div class="flex items-center gap-3 mb-2 flex-wrap">
                            <span class="font-medium">${l.name}</span>
                            <span class="text-xs px-2 py-1 rounded-full ${l.status==='new' ? 'badge-new' : 'badge-read'}">${l.status==='new' ? 'Новая' : 'Прочитано'}</span>
                        </div>
                        <div class="flex items-center gap-4 mb-3 flex-wrap">
                            <a href="tel:${l.phone}" class="text-sm text-brand-gray hover:text-white transition-colors" onclick="e=>e.stopPropagation()">${l.phone}</a>
                            ${l.email ? `<a href="mailto:${l.email}" class="text-sm text-brand-gray hover:text-white transition-colors">${l.email}</a>` : ''}
                        </div>
                        <p class="text-sm text-brand-gray leading-relaxed">${l.project}</p>
                    </div>
                </div>
                <div class="flex flex-col items-end gap-3 flex-shrink-0">
                    <span class="text-xs text-brand-gray">${l.date}</span>
                    <div class="flex items-center gap-2 opacity-0 group-hover:opacity-100 transition-opacity">
                        ${l.status === 'new' ? `<button onclick="event.stopPropagation();markRead(${l.id})" class="p-2 bg-brand-darker hover:bg-green-500/10 hover:text-green-400 transition-colors text-brand-gray rounded-sm" title="Отметить прочитанным">
                            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><polyline points="20 6 9 17 4 12"/></svg>
                        </button>` : ''}
                        <button onclick="event.stopPropagation();deleteLead(${l.id})" class="p-2 bg-brand-darker hover:bg-brand-red/10 hover:text-brand-red transition-colors text-brand-gray rounded-sm" title="Удалить">
                            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                                <polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 01-2 2H8a2 2 0 01-2-2L5 6"/><path d="M10 11v6M14 11v6"/><path d="M9 6V4h6v2"/>
                            </svg>
                        </button>
                    </div>
                </div>
            </div>
        </div>`).join('');
}

function markRead(id) {
    const leads = DB.get('leads', []);
    const l = leads.find(l => l.id === id);
    if (l) { l.status = 'read'; DB.set('leads', leads); renderLeads(); renderDashboard(); notify('Отмечено как прочитанное'); }
}

function deleteLead(id) {
    if (!confirm('Удалить заявку?')) return;
    DB.set('leads', DB.get('leads', []).filter(l => l.id !== id));
    renderLeads(); renderDashboard(); notify('Заявка удалена');
}

function clearAllLeads() {
    if (!confirm('Удалить ВСЕ заявки? Это действие необратимо.')) return;
    DB.set('leads', []); renderLeads(); renderDashboard(); notify('Все заявки удалены');
}

function openLeadDetail(id) {
    const l = DB.get('leads', []).find(l => l.id === id);
    if (!l) return;
    if (l.status === 'new') { markRead(id); }
    document.getElementById('leadDetail').innerHTML = `
        <div class="grid grid-cols-2 gap-4">
            <div class="space-y-1"><div class="text-xs text-brand-gray tracking-widests uppercase">Имя</div><div class="font-medium">${l.name}</div></div>
            <div class="space-y-1"><div class="text-xs text-brand-gray tracking-widests uppercase">Дата</div><div class="text-sm">${l.date}</div></div>
        </div>
        <div class="space-y-1"><div class="text-xs text-brand-gray tracking-widests uppercase">Телефон</div>
            <a href="tel:${l.phone}" class="text-brand-red hover:text-white transition-colors">${l.phone}</a></div>
        ${l.email ? `<div class="space-y-1"><div class="text-xs text-brand-gray tracking-widests uppercase">Email</div>
            <a href="mailto:${l.email}" class="text-brand-red hover:text-white transition-colors">${l.email}</a></div>` : ''}
        <div class="space-y-2"><div class="text-xs text-brand-gray tracking-widests uppercase">Описание проекта</div>
            <p class="text-sm leading-relaxed bg-brand-darker p-4 rounded-sm">${l.project}</p></div>
        <div class="flex gap-3 pt-4">
            <a href="tel:${l.phone}" class="flex-1 flex items-center justify-center gap-2 bg-brand-red py-3 text-sm text-white hover:bg-brand-red-accent transition-colors">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72 12.84 12.84 0 00.7 2.81 2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45 12.84 12.84 0 002.81.7A2 2 0 0122 16.92z"/></svg>
                Позвонить
            </a>
            ${l.email ? `<a href="mailto:${l.email}" class="flex-1 flex items-center justify-center gap-2 border border-white/20 py-3 text-sm hover:border-white/40 transition-colors">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
                Написать
            </a>` : ''}
        </div>`;
    document.getElementById('leadModal').classList.remove('hidden');
}

function exportLeads() {
    const leads = DB.get('leads', []);
    if (!leads.length) return notify('Нет заявок для экспорта', 'error');
    const headers = ['ID','Имя','Телефон','Email','Проект','Дата','Статус'];
    const rows = leads.map(l => [l.id, l.name, l.phone, l.email || '', `"${l.project}"`, l.date, l.status === 'new' ? 'Новая' : 'Прочитано']);
    const csv = [headers, ...rows].map(r => r.join(';')).join('\n');
    const blob = new Blob(['\uFEFF' + csv], { type: 'text/csv;charset=utf-8' });
    const a = document.createElement('a'); a.href = URL.createObjectURL(blob);
    a.download = `wilart_leads_${new Date().toISOString().slice(0,10)}.csv`; a.click();
    notify('CSV файл скачан');
}

// =============================================
//  PROJECTS
// =============================================
function renderProjects() {
    const projects = DB.get('projects', []);
    const container = document.getElementById('projectsGrid');
    if (!projects.length) { container.innerHTML = '<div class="col-span-2 glass rounded-sm p-12 text-center text-brand-gray">Проектов пока нет</div>'; return; }
    container.innerHTML = projects.map(p => `
        <div class="glass rounded-sm overflow-hidden group">
            <div class="aspect-video bg-brand-darker flex items-center justify-center">
                <div class="text-center p-6">
                    <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#333" stroke-width="0.8" stroke-linecap="round" class="mx-auto mb-3">
                        <rect x="3" y="3" width="18" height="18" rx="2"/>
                        <circle cx="8.5" cy="8.5" r="1.5"/><path d="M21 15l-5-5L5 21"/>
                    </svg>
                    <p class="text-brand-gray text-xs tracking-widests uppercase">${p.name}</p>
                </div>
            </div>
            <div class="p-6">
                <div class="flex items-start justify-between mb-4">
                    <div>
                        <div class="flex items-center gap-3 mb-2">
                            <span class="text-xs text-brand-red tracking-widests uppercase">${p.type}</span>
                            <span class="text-xs text-brand-gray">${p.year}</span>
                        </div>
                        <h3 class="font-serif text-xl text-white">${p.name}</h3>
                    </div>
                </div>
                <p class="text-brand-gray text-sm leading-relaxed mb-4">${p.desc}</p>
                <div class="grid grid-cols-2 gap-3 mb-4">
                    ${p.rooms ? `<div class="bg-brand-darker p-3 text-center"><div class="text-lg font-serif text-white">${p.rooms}</div><div class="text-xs text-brand-gray">номеров</div></div>` : ''}
                    ${p.days ? `<div class="bg-brand-darker p-3 text-center"><div class="text-lg font-serif text-white">${p.days}</div><div class="text-xs text-brand-gray">дней</div></div>` : ''}
                </div>
                <div class="flex flex-wrap gap-2 mb-5">
                    ${(p.tags || '').split(',').filter(Boolean).map(t => `<span class="px-3 py-1 bg-brand-darker text-brand-gray text-xs rounded-sm">${t.trim()}</span>`).join('')}
                </div>
                <div class="flex items-center gap-3">
                    <button onclick="editProject(${p.id})" class="flex-1 py-2.5 border border-white/20 text-xs tracking-widests uppercase hover:border-white/40 transition-colors">
                        Редактировать
                    </button>
                    <button onclick="deleteProject(${p.id})" class="p-2.5 border border-brand-red/30 text-brand-red hover:bg-brand-red/10 transition-colors">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                            <polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 01-2 2H8a2 2 0 01-2-2L5 6"/>
                            <path d="M10 11v6M14 11v6M9 6V4h6v2"/>
                        </svg>
                    </button>
                </div>
            </div>
        </div>`).join('');
}

function openAddProjectModal() {
    document.getElementById('projectModalTitle').textContent = 'Добавить проект';
    document.getElementById('projectForm').reset();
    document.getElementById('projectId').value = '';
    document.getElementById('projectModal').classList.remove('hidden');
    switchTab('projects');
}

function editProject(id) {
    const p = DB.get('projects', []).find(p => p.id === id);
    if (!p) return;
    document.getElementById('projectModalTitle').textContent = 'Редактировать проект';
    document.getElementById('projectId').value = p.id;
    document.getElementById('projectName').value = p.name;
    document.getElementById('projectType').value = p.type;
    document.getElementById('projectYear').value = p.year;
    document.getElementById('projectRooms').value = p.rooms;
    document.getElementById('projectDays').value = p.days;
    document.getElementById('projectDesc').value = p.desc;
    document.getElementById('projectTags').value = p.tags;
    document.getElementById('projectModal').classList.remove('hidden');
}

function saveProject(e) {
    e.preventDefault();
    const projects = DB.get('projects', []);
    const id = document.getElementById('projectId').value;
    const data = {
        id: id ? parseInt(id) : Date.now(),
        name: document.getElementById('projectName').value,
        type: document.getElementById('projectType').value,
        year: parseInt(document.getElementById('projectYear').value),
        rooms: parseInt(document.getElementById('projectRooms').value) || 0,
        days: parseInt(document.getElementById('projectDays').value) || 0,
        desc: document.getElementById('projectDesc').value,
        tags: document.getElementById('projectTags').value,
    };
    if (id) {
        const idx = projects.findIndex(p => p.id === parseInt(id));
        if (idx !== -1) projects[idx] = data;
    } else {
        projects.push(data);
    }
    DB.set('projects', projects);
    closeModal('projectModal');
    renderProjects();
    renderDashboard();
    notify(id ? 'Проект обновлён' : 'Проект добавлен');
}

function deleteProject(id) {
    if (!confirm('Удалить проект?')) return;
    DB.set('projects', DB.get('projects', []).filter(p => p.id !== id));
    renderProjects(); renderDashboard(); notify('Проект удалён');
}

// =============================================
//  REVIEWS
// =============================================
function renderReviews() {
    const reviews = DB.get('reviews', []);
    const container = document.getElementById('reviewsGrid');
    if (!reviews.length) { container.innerHTML = '<div class="col-span-3 glass rounded-sm p-12 text-center text-brand-gray">Отзывов пока нет</div>'; return; }
    container.innerHTML = reviews.map(r => `
        <div class="glass rounded-sm p-8">
            <div class="flex items-center gap-1 mb-5">
                ${'★'.repeat(r.stars)}<span class="text-brand-gray text-sm ml-1">${r.stars}/5</span>
            </div>
            <blockquote class="text-brand-gray leading-relaxed text-sm italic mb-6">«${r.text}»</blockquote>
            <div class="flex items-center gap-3 pt-4 border-t border-white/5 mb-5">
                <div class="w-10 h-10 rounded-full bg-brand-darker flex items-center justify-center text-brand-gold text-sm font-serif">
                    ${r.name.split(' ').map(n=>n[0]).join('').slice(0,2)}
                </div>
                <div>
                    <div class="text-sm font-medium">${r.name}</div>
                    <div class="text-xs text-brand-gray">${r.position}</div>
                </div>
            </div>
            <div class="flex items-center gap-3">
                <button onclick="editReview(${r.id})" class="flex-1 py-2 border border-white/20 text-xs tracking-widests uppercase hover:border-white/40 transition-colors">
                    Редактировать
                </button>
                <button onclick="deleteReview(${r.id})" class="p-2 border border-brand-red/30 text-brand-red hover:bg-brand-red/10 transition-colors">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                        <polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 01-2 2H8a2 2 0 01-2-2L5 6"/>
                        <path d="M10 11v6M14 11v6M9 6V4h6v2"/>
                    </svg>
                </button>
            </div>
        </div>`).join('');
}

function openAddReviewModal() {
    document.getElementById('reviewModalTitle').textContent = 'Добавить отзыв';
    document.getElementById('reviewForm').reset();
    document.getElementById('reviewId').value = '';
    document.getElementById('reviewModal').classList.remove('hidden');
}

function editReview(id) {
    const r = DB.get('reviews', []).find(r => r.id === id);
    if (!r) return;
    document.getElementById('reviewModalTitle').textContent = 'Редактировать отзыв';
    document.getElementById('reviewId').value = r.id;
    document.getElementById('reviewName').value = r.name;
    document.getElementById('reviewPosition').value = r.position;
    document.getElementById('reviewStars').value = r.stars;
    document.getElementById('reviewText').value = r.text;
    document.getElementById('reviewModal').classList.remove('hidden');
}

function saveReview(e) {
    e.preventDefault();
    const reviews = DB.get('reviews', []);
    const id = document.getElementById('reviewId').value;
    const data = {
        id: id ? parseInt(id) : Date.now(),
        name: document.getElementById('reviewName').value,
        position: document.getElementById('reviewPosition').value,
        stars: parseInt(document.getElementById('reviewStars').value),
        text: document.getElementById('reviewText').value,
    };
    if (id) { const idx = reviews.findIndex(r => r.id === parseInt(id)); if (idx !== -1) reviews[idx] = data; }
    else reviews.push(data);
    DB.set('reviews', reviews);
    closeModal('reviewModal');
    renderReviews();
    renderDashboard();
    notify(id ? 'Отзыв обновлён' : 'Отзыв добавлен');
}

function deleteReview(id) {
    if (!confirm('Удалить отзыв?')) return;
    DB.set('reviews', DB.get('reviews', []).filter(r => r.id !== id));
    renderReviews(); renderDashboard(); notify('Отзыв удалён');
}

// =============================================
//  CONTENT
// =============================================
function loadContent() {
    const c = DB.get('content', {});
    const fields = ['heroTitle','heroSubtitle','heroBadge','heroCTA','statYears','statObjects','statArea','statStaff','aboutTitle','aboutText1','aboutText2','contactPhone','contactEmail','contactAddress','notifyEmail'];
    fields.forEach(id => { if (c[id] !== undefined && document.getElementById(id)) document.getElementById(id).value = c[id]; });
}

function saveContent() {
    const fields = ['heroTitle','heroSubtitle','heroBadge','heroCTA','statYears','statObjects','statArea','statStaff','aboutTitle','aboutText1','aboutText2','contactPhone','contactEmail','contactAddress','notifyEmail'];
    const c = {};
    fields.forEach(id => { if (document.getElementById(id)) c[id] = document.getElementById(id).value; });
    DB.set('content', c);
    notify('Контент сохранён! Примените изменения на сайте.');
}

// =============================================
//  MODAL UTILS
// =============================================
function closeModal(id) { document.getElementById(id).classList.add('hidden'); }
document.addEventListener('keydown', e => { if (e.key === 'Escape') { ['projectModal','reviewModal','leadModal'].forEach(closeModal); }});
['projectModal','reviewModal','leadModal'].forEach(id => {
    document.getElementById(id)?.addEventListener('click', e => { if (e.target.id === id) closeModal(id); });
});

// =============================================
//  DANGER ZONE
// =============================================
function resetAllData() {
    if (!confirm('ВНИМАНИЕ! Это удалит все данные (заявки, проекты, отзывы). Продолжить?')) return;
    if (!confirm('Вы уверены? Данные невозможно восстановить!')) return;
    ['leads','projects','reviews','content'].forEach(k => localStorage.removeItem('wilart_' + k));
    DB.set('leads', DEFAULT_LEADS);
    DB.set('projects', DEFAULT_PROJECTS);
    DB.set('reviews', DEFAULT_REVIEWS);
    renderDashboard(); renderLeads(); renderProjects(); renderReviews(); loadContent();
    notify('Данные сброшены до заводских настроек');
}

// =============================================
//  DEMO: Simulate incoming lead
// =============================================
window.addDemoLead = function(name, phone, email, project) {
    const leads = DB.get('leads', []);
    leads.push({
        id: Date.now(),
        name: name || 'Новый клиент',
        phone: phone || '+7 (000) 000-00-00',
        email: email || '',
        project: project || 'Запрос с сайта',
        date: new Date().toLocaleString('ru-RU', {year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'}).replace(',',''),
        status: 'new'
    });
    DB.set('leads', leads);
    renderLeads(); renderDashboard();
    notify('🔔 Новая заявка получена!');
};

// =============================================
//  INIT on page load
// =============================================
document.addEventListener('DOMContentLoaded', () => {
    const isLoggedIn = sessionStorage.getItem('wilart_admin');
    if (isLoggedIn) {
        document.getElementById('loginScreen').style.display = 'none';
        document.getElementById('adminLayout').classList.remove('hidden');
        initAdmin();
    }
    document.getElementById('loginForm')?.addEventListener('submit', function(e) {
        sessionStorage.setItem('wilart_admin', '1');
    });
});
</script>
</body>
</html>
```

---


> 💡 **Интеграция с сайтом**: Чтобы данные из формы автоматически попадали в админку, добавьте в `index.html` в функцию `handleSubmit` код сохранения в `localStorage` — и заявки появятся в панели мгновенно.
